{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch AdventOfCode2024",
            "program": "${workspaceFolder}/target/debug/day${input:dayNumber}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "dayNumber",
            "description": "Day number to run"
        }
    ]
}